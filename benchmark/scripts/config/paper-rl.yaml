llm:
  api_base: ""
  api_key: ""
  model_id: Qwen/Qwen3-8B
  provider: custom
mode: rl
proxy:
  host: 0.0.0.0
  port: 30000
rl:
  enabled: true
  model: Qwen/Qwen3-8B
  tinker_api_key: ${TINKER_KEY}
  prm_url: ${OPENAI_BASE_URL}
  prm_model: ${PRM_MODEL}
  prm_api_key: ${OPENAI_API_KEY}
  manual_train_trigger: false
  batch_size: 8
  lora_rank: 32
  evolver_api_base: ${OPENAI_BASE_URL}
  evolver_api_key: ${OPENAI_API_KEY}
  evolver_model: ${PRM_MODEL}
scheduler:
  calendar:
    enabled: false
  enabled: false
skills:
  auto_evolve: true
  dir: ${METACLAW_ROOT}/memory_data/skills
  enabled: true
  retrieval_mode: template
  task_specific_top_k: 10
  top_k: 6
memory:
  enabled: false
max_context_tokens: 50000
